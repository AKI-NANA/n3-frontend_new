-- ============================================================================
-- NAGANO-3 データベース完全調査（安全版）
-- ============================================================================
-- まず存在するテーブルを確認してから、そのテーブルだけを調査
-- ============================================================================

-- ============================================================================
-- STEP 1: 存在する全テーブルを確認
-- ============================================================================
SELECT 
    tablename,
    schemaname
FROM pg_tables
WHERE schemaname = 'public'
ORDER BY tablename;

-- 👆 この結果を確認してから、存在するテーブルだけ以下のクエリを実行してください
