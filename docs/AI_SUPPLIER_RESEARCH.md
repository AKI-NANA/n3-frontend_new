# AI仕入れ先特定機能 - 実装ドキュメント

## 📋 概要

この機能は、eBayリサーチで見つかった商品について、AIが国内ECサイト（Amazon Japan、楽天、Yahoo!ショッピングなど）から最安値の仕入れ先を自動探索し、最適な仕入れ原価を特定します。

## 🎯 目的

1. **仕入れ先候補の自動探索**: 商品名・型番、画像解析、DBとの照合により、最安値の仕入れ先を特定
2. **利益計算の最適化**: 特定された仕入れ原価を使用して、正確な利益計算とスコアリングを実現
3. **出品可否の判断支援**: 最終Uiスコアに基づき、出品すべき商品を効率的に選別

## 🏗️ アーキテクチャ

### データフロー

```
1. eBayリサーチ実行
   ↓
2. リサーチ結果管理UI（/research/results-management）
   - フィルタリング
   - 暫定Uiスコア表示
   - AI解析対象の選別
   ↓
3. AI仕入れ先探索キュー（/api/research/ai-queue）
   - キューへの追加
   - 非同期処理の開始
   ↓
4. AI仕入れ先特定モジュール（SupplierLocator）
   - 商品名/型番検索
   - 画像解析
   - DB照合
   ↓
5. 仕入れ先候補の保存（SupplierDatabaseService）
   - supplier_candidates テーブルに保存
   - products_master の更新
   ↓
6. 最終スコア計算（supplier-cost-integrator）
   - AI特定原価を使用
   - 信頼度ペナルティの適用
   ↓
7. 承認画面（/approval）
   - 最終Uiスコアで承認判断
```

## 📊 データベース設計

### 新規テーブル

#### `supplier_candidates` - 仕入れ先候補

| カラム名 | 型 | 説明 |
|---------|---|------|
| id | UUID | 主キー |
| product_id | TEXT | 商品ID（products_master） |
| supplier_url | TEXT | 仕入れ先URL |
| supplier_platform | TEXT | プラットフォーム（amazon_jp, rakuten等） |
| candidate_price_jpy | NUMERIC | 本体価格（税抜） |
| estimated_domestic_shipping_jpy | NUMERIC | 推定国内送料 |
| total_cost_jpy | NUMERIC | 総仕入れコスト（計算値） |
| confidence_score | NUMERIC | 信頼度（0.0〜1.0） |
| matching_method | TEXT | マッチング手法 |
| in_stock | BOOLEAN | 在庫有無 |

#### `ai_research_queue` - AI解析キュー

| カラム名 | 型 | 説明 |
|---------|---|------|
| id | UUID | 主キー |
| product_id | TEXT | 商品ID |
| status | TEXT | QUEUED, PROCESSING, COMPLETED, FAILED |
| priority | INTEGER | 優先度（大きいほど高優先） |
| queued_at | TIMESTAMPTZ | キュー追加日時 |
| suppliers_found | INTEGER | 発見された候補数 |
| error_message | TEXT | エラーメッセージ |

### 既存テーブルへの追加カラム

#### `products_master` 拡張

| カラム名 | 型 | 説明 |
|---------|---|------|
| research_status | TEXT | NEW, SCORED, AI_QUEUED, AI_COMPLETED, VERIFIED |
| ai_cost_status | BOOLEAN | AI仕入れ先特定完了フラグ |
| provisional_ui_score | NUMERIC | 暫定Uiスコア |
| final_ui_score | NUMERIC | 最終Uiスコア |
| best_supplier_id | UUID | 最良仕入れ先ID |
| last_research_date | TIMESTAMPTZ | 最終リサーチ日時 |

## 🛠️ 実装ファイル

### バックエンド

| ファイルパス | 説明 |
|-------------|------|
| `/services/ai_pipeline/SupplierLocator.ts` | AI仕入れ先特定モジュール（コアロジック） |
| `/services/ai_pipeline/SupplierDatabaseService.ts` | DB連携サービス |
| `/lib/scoring/supplier-cost-integrator.ts` | スコア計算統合ロジック |
| `/app/api/research/ai-queue/route.ts` | AI解析キューAPI |

### フロントエンド

| ファイルパス | 説明 |
|-------------|------|
| `/app/research/results-management/page.tsx` | リサーチ結果管理UI |
| `/components/research/AISendSelector.tsx` | AI解析対象選別コンポーネント |

### 型定義

| ファイルパス | 説明 |
|-------------|------|
| `/types/supplier.ts` | 仕入れ先関連の型定義 |

### データベース

| ファイルパス | 説明 |
|-------------|------|
| `/supabase/migrations/20251122_add_supplier_research.sql` | マイグレーションSQL |

## 🚀 使い方

### 1. DBマイグレーションの実行

```sql
-- Supabase管理画面のSQL Editorで実行
-- /supabase/migrations/20251122_add_supplier_research.sql の内容を実行
```

### 2. リサーチ結果の管理

1. `/research/results-management` にアクセス
2. フィルターでリサーチ結果を絞り込み
3. 暫定Uiスコアを確認

### 3. AI解析対象の選別

**選別方法:**
- **上位X%選択**: 暫定スコア上位10%など、パーセンテージで選別
- **スコア閾値**: 60000点以上など、最低ラインで選別
- **クイック選択**: 上位5%, 10%, 25%, 70k以上のボタン

**推奨設定:**
- 初回: 上位10%を選択（効率重視）
- 有望商品: スコア60k以上（品質重視）

### 4. AI解析キューへ送信

1. 選別した商品をチェック
2. 優先度を設定（通常/やや高/高/最高）
3. 「AI解析キューへ送信」ボタンをクリック

### 5. バックグラウンド処理

- キュー送信後、自動でAI解析が開始
- `research_status` が `AI_QUEUED` → `PROCESSING` → `AI_COMPLETED` に変化
- 処理完了後、`final_ui_score` が計算される

### 6. 承認画面で最終判断

- `/approval` にアクセス
- `final_ui_score` で並び替え
- 仕入れ先URL、価格、信頼度を確認
- 承認/拒否の判断

## 🔧 API エンドポイント

### POST `/api/research/ai-queue`

商品をAI解析キューに追加

**リクエスト:**
```json
{
  "product_ids": ["product_1", "product_2"],
  "priority": 10,
  "requested_by": "user_id"
}
```

**レスポンス:**
```json
{
  "success": true,
  "queued_count": 2,
  "queue_items": [...]
}
```

### GET `/api/research/ai-queue?status=QUEUED`

キューの状態を取得

**レスポンス:**
```json
{
  "success": true,
  "queue_items": [...],
  "total": 10
}
```

### DELETE `/api/research/ai-queue?product_id=xxx`

キューアイテムをキャンセル

## 📈 スコア計算ロジック

### 暫定Uiスコア（Provisional Score）

仕入れ先が未確定の場合:
- 既知データ（SOLD数、競合数など）のみで計算
- 利益スコアは低めに見積もり
- 5%のペナルティを適用

### 最終Uiスコア（Final Score）

AI特定の仕入れ原価を使用:
```
最終スコア = 基本スコア × 信頼度ペナルティ

信頼度ペナルティ:
- 信頼度 >= 0.8: ペナルティなし (1.0)
- 信頼度 >= 0.6: 5%ペナルティ (0.95)
- 信頼度 >= 0.4: 10%ペナルティ (0.9)
- 信頼度 < 0.4: 20%ペナルティ (0.8)
```

## 🎨 AI探索ロジック

### 優先順位1: 商品名・型番検索

- 商品名、英語タイトル、型番から検索キーワード生成
- 各プラットフォームのAPI/スクレイピングで検索
- タイトル類似度でマッチング

### 優先順位2: 画像解析

- 商品画像を使用
- Google Lens API、SerpApiなどの画像検索
- 類似画像から商品を特定

### 優先順位3: DB照合

- 内部の優良仕入れ先データベースと照合
- 過去の取引実績から推奨仕入れ先を提示

## 🔒 信頼度スコアリング

### マッチング手法による信頼度

- **database_match**: +0.3（高信頼）
- **title_match**: +0.2（中信頼）
- **image_search**: +0.1（低信頼）

### 類似度スコアによる調整

- タイトル/画像の類似度を0.0〜1.0で評価
- 信頼度 = ベース信頼度 × 0.5 + 類似度 × 0.5

### 在庫状況による調整

- 在庫切れの場合: 信頼度 × 0.7

## ⚙️ 設定とカスタマイズ

### SupplierLocator設定

```typescript
const config = {
  enableTitleSearch: true,      // 商品名検索
  enableImageSearch: true,      // 画像検索
  enableDatabaseSearch: true,   // DB照合
  maxCandidates: 10,            // 最大候補数
  minConfidenceThreshold: 0.3,  // 最低信頼度
  estimatedShippingJpy: 500,    // デフォルト送料
};
```

### プラットフォーム設定

```typescript
const platforms = [
  'amazon_jp',
  'rakuten',
  'yahoo_shopping',
  'mercari',
];
```

## 📝 TODO: 実際のAPI統合

現在はモック実装のため、以下のAPI統合が必要:

### Amazon Product Advertising API

```typescript
// services/ai_pipeline/integrations/AmazonAPI.ts
async function searchAmazonJP(keywords: string[]) {
  // Amazon PA-API 実装
}
```

### 楽天市場API

```typescript
// services/ai_pipeline/integrations/RakutenAPI.ts
async function searchRakuten(keywords: string[]) {
  // 楽天商品検索API 実装
}
```

### Google Lens / SerpApi

```typescript
// services/ai_pipeline/integrations/ImageSearchAPI.ts
async function reverseImageSearch(imageUrl: string) {
  // 画像検索API 実装
}
```

## 🐛 トラブルシューティング

### キューが処理されない

- `ai_research_queue` テーブルで `status = 'PROCESSING'` のまま停止
- → `/api/research/ai-queue` を再度呼び出し、バックグラウンド処理を再起動

### 信頼度が低い

- AI特定の価格が不正確
- → 手動で `supplier_candidates.verified_by_human = true` にマーク
- → `products_master.best_supplier_id` を手動で修正

### スコアが計算されない

- `research_status` が `AI_COMPLETED` だがスコアが0
- → `/api/score/calculate` を手動実行

## 📚 参考資料

- [Supabase公式ドキュメント](https://supabase.com/docs)
- [Amazon Product Advertising API](https://webservices.amazon.com/paapi5/documentation/)
- [楽天市場API](https://webservice.rakuten.co.jp/)
- [SerpApi Google Lens](https://serpapi.com/google-lens-api)

## 🎉 完成機能

✅ DB設計とマイグレーション
✅ AI仕入れ先特定モジュール
✅ DB連携サービス
✅ スコア計算統合
✅ AI解析キューAPI
✅ リサーチ結果管理UI
✅ AI解析対象選別コンポーネント

## 🚧 今後の拡張予定

- [ ] 実際のEC API統合（Amazon, 楽天, Yahoo!）
- [ ] 画像検索API統合（Google Lens, SerpApi）
- [ ] VPSでのcronジョブ化（定期的なキュー処理）
- [ ] 仕入れ先候補の人間による検証UI
- [ ] 仕入れ先の自動発注連携
- [ ] 価格変動の自動追跡
