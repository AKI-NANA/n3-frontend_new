# 大量データ対応: AI-Supabase直接連携

## 🚨 現在の設計の致命的な問題

### プロンプトサイズの限界

```
HTSコード: 17,000件
1件あたり: 約30文字
合計: 約510,000文字

Gemini制限: 32,000文字 ❌
Claude Web制限: 200,000文字 ❌
→ どちらも不可能！
```

---

## ✅ 正しい解決策: AIがSupabaseに直接アクセス

### 1. Claude Desktop + MCP（推奨）

```
Claude Desktop（ローカル）
    ↓ MCP経由
Supabase
    ↓ SQL実行可能
17,000件のHTSコードを直接検索
```

**メリット**:
- ✅ 無制限のデータアクセス
- ✅ 高速検索
- ✅ コスト: ¥0（完全無料）

**設定方法**:

```json
// ~/Library/Application Support/Claude/claude_desktop_config.json
{
  "mcpServers": {
    "supabase": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-postgres",
        "postgresql://postgres:[PASSWORD]@db.[PROJECT].supabase.co:5432/postgres"
      ]
    }
  }
}
```

---

### 2. Claude API + MCP（有料だが完全自動）

```
Next.js
    ↓ POST /api/claude-enrichment
Claude API（¥7.5/回）
    ↓ MCP経由
Supabase
    ↓ 17,000件検索可能
自動処理完了
```

**メリット**:
- ✅ 完全自動化
- ✅ 無制限のデータアクセス
- ❌ コスト: ¥7.5/商品

---

### 3. 他のAI + データベース連携

#### Cursor AI（推奨・無料プランあり）
```
Cursor AI
    ↓ PostgreSQL接続
Supabase
    ↓ 直接SQL実行
17,000件検索可能
```

**特徴**:
- ✅ 無料プランあり
- ✅ PostgreSQL直接接続
- ✅ コード生成特化

**設定**:
```javascript
// Cursorで実行
const { createClient } = require('@supabase/supabase-js')

const supabase = createClient(
  'https://[PROJECT].supabase.co',
  '[API_KEY]'
)

// 17,000件を直接検索可能
const { data } = await supabase
  .from('hs_codes')
  .select('*')
  .ilike('description', '%camera%')
```

#### GitHub Copilot + Database（有料）
```
VS Code + Copilot
    ↓ Database Extension
Supabase
    ↓ SQL実行
17,000件検索可能
```

**特徴**:
- ❌ 有料（$10/月）
- ✅ PostgreSQL接続
- ✅ IDE統合

---

## 🎯 推奨ソリューション

### プラン A: Claude Desktop + MCP（完全無料）

```
┌─────────────────────────────────────────┐
│  Claude Desktop（ローカルアプリ）        │
│  - MCP Supabase接続                     │
│  - 完全無料                             │
│  - 17,000件検索可能                     │
└─────────────────────────────────────────┘
    ↓ ユーザーが会話形式で依頼
┌─────────────────────────────────────────┐
│  商品: Canon EOS カメラ三脚              │
│                                         │
│  Claude: Supabaseのhs_codesテーブルで   │
│  "camera tripod"で検索します            │
│                                         │
│  SELECT * FROM hs_codes                 │
│  WHERE description ILIKE '%camera%'     │
│    OR description ILIKE '%tripod%'      │
│  LIMIT 10                               │
│                                         │
│  結果: 9006.91.0000 が最適です           │
└─────────────────────────────────────────┘
    ↓ 自動的にSupabase検索
┌─────────────────────────────────────────┐
│  customs_dutiesテーブルから関税率取得    │
│  WHERE hts = '9006.91.0000'             │
│    AND country = 'CN'                   │
│  → 10.25%                               │
└─────────────────────────────────────────┘
```

**メリット**:
- ✅ コスト: ¥0
- ✅ 17,000件全てアクセス可能
- ✅ リアルタイム検索
- ✅ 手動介入（ユーザーが確認）

**デメリット**:
- ⚠️ 完全自動ではない（会話形式）
- ⚠️ Next.jsシステムとは別

---

### プラン B: システム統合版（段階的絞り込み）

```
┌─────────────────────────────────────────┐
│  ステップ1: カテゴリ絞り込み             │
│  Next.js → Supabase                     │
│  商品カテゴリ推定 → 候補を200件に絞る    │
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│  ステップ2: AI判定（無料AI使用可能）     │
│  200件の候補をプロンプトに含める         │
│  Gemini/Claude Web で処理可能           │
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│  ステップ3: 関税率取得                   │
│  Next.js → Supabase                     │
│  customs_dutiesテーブルで検索            │
└─────────────────────────────────────────┘
```

**絞り込みロジック**:

```javascript
// app/api/hts-codes-filtered/route.ts

export async function POST(request: Request) {
  const { productTitle, categoryId } = await request.json()
  
  // ステップ1: カテゴリマッピング
  const categoryMap = {
    '293': ['9006', '8529'],  // カメラ関連
    '625': ['6204', '6203'],  // アパレル
    '58058': ['8517', '8471'], // 電子機器
  }
  
  const prefixes = categoryMap[categoryId] || []
  
  // ステップ2: タイトルからキーワード抽出
  const keywords = extractKeywords(productTitle)
  // "Canon EOS カメラ三脚" → ["camera", "tripod"]
  
  // ステップ3: Supabaseで絞り込み検索
  const { data } = await supabase
    .from('hs_codes')
    .select('*')
    .or(
      prefixes.map(p => `code.like.${p}%`).join(',')
    )
    .or(
      keywords.map(k => `description.ilike.%${k}%`).join(',')
    )
    .limit(200)
  
  return Response.json(data)
}

function extractKeywords(title: string): string[] {
  // 日本語 → 英語翻訳（簡易版）
  const dict = {
    'カメラ': 'camera',
    '三脚': 'tripod',
    'レンズ': 'lens',
    // ... 主要なキーワード1000語
  }
  
  const words = title.split(/\s+/)
  return words
    .map(w => dict[w] || w)
    .filter(w => w.length > 2)
}
```

---

## 🔧 実装比較

### 方式1: Claude Desktop + MCP（完全無料）

```bash
# 1. MCPサーバー設定
cat > ~/Library/Application\ Support/Claude/claude_desktop_config.json << 'EOF'
{
  "mcpServers": {
    "supabase": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-postgres",
        "postgresql://postgres:[PASSWORD]@db.[PROJECT].supabase.co:5432/postgres"
      ]
    }
  }
}
EOF

# 2. Claude Desktopを再起動
# 3. 会話開始
```

**使用例**:
```
ユーザー: この商品のHTSコードを調べて
商品名: Canon EOS カメラ三脚

Claude: hs_codesテーブルで検索します
[SQLクエリ実行]
結果: 9006.91.0000 (camera tripods) が最適です

次にcustoms_dutiesで関税率を調べます
[SQLクエリ実行]
CN（中国）からの輸入の場合: 10.25%
```

**コスト**: ¥0

---

### 方式2: カテゴリ絞り込み + 無料AI

```typescript
// フロー
1. Next.js: eBayカテゴリIDから絞り込み
   17,000件 → 200件

2. プロンプト生成: 200件を含む

3. Gemini/Claude Web: 200件から選択

4. Next.js: 関税率取得
```

**コスト**: ¥0

---

### 方式3: Claude API（完全自動・有料）

```typescript
// app/api/claude-enrichment/route.ts

export async function POST(request: Request) {
  const { productId } = await request.json()
  
  // Claude APIに依頼（MCP使用）
  const response = await anthropic.messages.create({
    model: 'claude-3-5-sonnet-20241022',
    max_tokens: 4096,
    tools: [
      {
        type: 'computer_20241022',
        name: 'supabase',
        // MCPサーバー経由でSupabase接続
      }
    ],
    messages: [{
      role: 'user',
      content: `
        商品ID ${productId} のHTSコードを判定してください。
        
        1. hs_codes テーブルで適切なコードを検索
        2. 原産国を判定
        3. customs_duties テーブルで関税率を取得
        4. products テーブルを更新
      `
    }]
  })
  
  return Response.json({ success: true })
}
```

**コスト**: ¥7.5/商品

---

## 📊 推奨度比較

| 方式 | コスト | 自動化 | データ量 | 設定難易度 |
|-----|-------|--------|---------|-----------|
| **Claude Desktop + MCP** | ¥0 | ⚠️ 手動 | ✅ 無制限 | ⭐⭐ 簡単 |
| **カテゴリ絞り込み** | ¥0 | ✅ 半自動 | ⚠️ 200件 | ⭐⭐⭐ 中程度 |
| **Claude API + MCP** | ¥7.5 | ✅ 完全自動 | ✅ 無制限 | ⭐⭐⭐⭐ 複雑 |
| **Cursor AI** | ¥0 | ⚠️ 手動 | ✅ 無制限 | ⭐⭐ 簡単 |

---

## 🎯 最終推奨

### 短期（今すぐ使える）: Claude Desktop + MCP
```
1. MCPサーバー設定（5分）
2. Claude Desktop起動
3. 会話形式でHTSコード判定
4. 結果をNext.jsシステムに手入力

メリット:
✅ 完全無料
✅ 17,000件全検索可能
✅ 設定簡単
```

### 中期（開発必要）: カテゴリ絞り込み
```
1. カテゴリマッピング実装
2. キーワード抽出実装
3. 絞り込みAPI実装

メリット:
✅ 完全無料
✅ システム統合
✅ 無料AI使用可能
```

### 長期（予算あり）: Claude API
```
1. MCP統合
2. Claude API実装
3. バッチ処理実装

メリット:
✅ 完全自動
✅ 高精度
❌ コスト: ¥7.5/商品
```

---

## 🚀 即座に始められる方法

### Claude Desktop + MCP設定（5分）

```bash
# 1. 設定ファイル作成
mkdir -p ~/Library/Application\ Support/Claude
cat > ~/Library/Application\ Support/Claude/claude_desktop_config.json << 'EOF'
{
  "mcpServers": {
    "supabase": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-postgres",
        "postgresql://postgres:あなたのパスワード@db.zdzfpucdyxdlavkgrvil.supabase.co:5432/postgres"
      ]
    }
  }
}
EOF

# 2. Claude Desktopを再起動

# 3. テスト
# Claude Desktopで:
# "hs_codesテーブルからカメラ関連のHTSコードを10件取得して"
```

---

## 📝 結論

あなたの指摘は完全に正しいです：

1. ✅ **17,000件をプロンプトに含めるのは不可能**
2. ✅ **AIがデータベースを直接読む必要がある**
3. ✅ **Claude Desktopは既にSupabaseと連携している**
4. ✅ **最適解: Claude Desktop + MCP（無料）**

**今すぐ使える無料ソリューション**: Claude Desktop + MCP
**システム統合版**: カテゴリ絞り込み + 無料AI
**完全自動版**: Claude API + MCP（有料）
